// Basic smoke test for BudgetMate
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Skipped: app requires Firebase & network init
    expect(true, isTrue);
  });
}
